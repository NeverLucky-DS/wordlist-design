# PLANS — очередь работ и открытые проблемы

> **Для чего этот файл.** Владелец не всегда за клавиатурой, а фоновые прогоны
> идут хорошо. Значит здесь всегда должно лежать больше работы, чем есть времени:
> взял верхний неблокированный пункт — сделал — вычеркнул.
>
> **Правила ведения** (продублированы в `CLAUDE.md`):
> - нашёл проблему по дороге и не чинишь сейчас → строчка в «Открытые проблемы»,
>   с замером, а не с ощущением;
> - план подтверждён владельцем → переносится в «Подтверждено» с оценкой объёма;
> - сделал → в «Сделано» одной строкой со ссылкой на коммит, старое подчищать;
> - каждый пункт должен читаться **на холодную**: что, зачем, где файлы, чем
>   проверить. Без этого он не пункт, а напоминание.

Обновлено: 2026-07-19.

---

## 🔴 Открытые проблемы

### 0. ✅ ОТРАБОТАН — замер покрытия: 82 % промахов были от ИСТОЧНИКА (2026-07-19)

> **Итог того же дня:** источник пополнен, слова обогащены, замер повторён на
> том же корпусе — **72.3 → 76.4 % по токенам, 65.5 → 70.4 % по типам**.
> Подробности и разбор оставшихся промахов — в пункте A. Ниже сохранён исходный
> замер: он объясняет, ПОЧЕМУ решение было «чинить вход», а не «чинить модель».

Первый честный тест базы не на себе самой, а на внешнем корпусе: 18 полных
статей de.wikipedia ровно по темам Goethe Schreiben (KI, Datenschutz, соцсети,
климат, цифровизация, миграция…), 177 КБ, 21 114 токенов.
Скрипты замера — `coverage.py`/`coverage2.py` (в скретчпаде, воспроизводимы за минуту).

| метрика | значение |
|---|---|
| покрытие по токенам, всё подряд | 70.4 % |
| покрытие по знаменательным словам | 72.3 % по токенам / 65.5 % по типам |
| промахи zipf ≥ 3.0 | 1 088 типов |
| **из них нет в `vocab.db` вовсе** | **895 (82 %)** |
| из них есть, но `skipped`/`failed` | 193 (18 %) |

**Вывод: узкое место — источник, а не обогащение.** Обогащение отработало
хорошо; чинить в нём почти нечего. Это переводит пункт A из «подтверждено» в
«главный приоритет» и даёт ему нормальное обоснование вместо оценки на глаз.

Пойманы поимённо отсутствующие слова, которые для тренажёра B1–C1 обязательны:
`Internet` (zipf 5.23!), `online/Online` (5.38), `aufgrund` (5.16),
`Digitalisierung`, `Klimawandel`, `Smartphone`, `Privatsphäre`, `Suchmaschine`,
`herunterladen`, `Videokonferenz`, `Homeoffice`, `Migrationshintergrund`.
Ни одного из них нет даже в `word_status` — то есть они никогда не были
кандидатами. Подтверждает [[vocab-source-is-pre-1995-dictionary]] замером.

Отдельно: `Potential` в базе есть, `Potenzial` (написание после 1996 и
единственное нормативное сегодня) — нет. Реформа орфографии чинилась только в
сторону ß→ss; ti→zi не покрыт.

### 0b. ✅ ЗАКРЫТО — закрытый класс служебных слов был утерян терминально

`das` в базе есть, а `der`, `die`, `den`, `dem`, `des` — нет: `die/den/dem` →
`skipped`, `der/des` → `failed` после 3 попыток. Оба статуса терминальные, сами
не вернутся. Так же потеряны слитные предлоги `im`, `am`, `zum`, `zur`, `vom`,
`beim`, `ins` и местоимения `euch`, `denen`, `aller`.

Это самые частотные слова языка (`die` — zipf 7.48), и они **не выводятся**
учеником из базовой формы: `im` = `in dem` надо просто знать, это грамматика
A1 из любого учебника.

⚠️ Причина — правило про Wortform в промпте: модель справедливо считает `die`
формой `der`, а `im` — стяжением. Правило в целом работает верно (в `skipped`
осели `ist`, `hat`, `war`, `meine` — и это правильно, базовые леммы на месте).
Ошибка только на закрытом классе.

Чинить **не обогащением, а списком руками**: набор конечный, ноль токенов
Mistral. Пытаться получить их от модели — воевать с правилом, которое в
остальном полезно.

**Сделано:** `backend/app/vocab/funcwords.py`, 25 карточек, сеются из
`plan_repairs()`. Проверено на живой базе — поиск отдаёт их первыми
(`der`→артикль вместо `derb`, `im`→«in + dem» вместо `Imam`, `zur` вместо
`Zuruf`, `euch` вместо `Eucharistie`). Барьер в `requeue` не даёт
`repair_ortho` удалить их на следующем прогоне — см. `CRITICAL-LINKS.md`.

### 0d. ✅ ЗАКРЫТО — `periodic_sync` работает, тревога была ложной

Гипотеза была, что `asyncio.create_task(mirror.periodic_sync())` падает до
входа в `while True`, и исключение некому забрать.

**Опровергнуто прогоном 19-го июля.** Весь прогон зеркало тянуло карточки само,
ровным ритмом, без единого ручного пинка:

```
09:33 +264 (total 84 889)   …   10:19 +714 (total 91 568)
09:38 +775 (total 85 664)       10:24 +522 (total 92 090)
```

Итог сошёлся побайтово: 92 090 карточек в SQLite = 92 090 в Postgres, zipf
заполнен у 92 090 из 92 090.

Что было на самом деле 12 минут при посеве служебных слов — не выяснено и
выяснять уже нечего: между тем наблюдением и прогоном контейнер пересобирался.
Правдоподобно, что старый процесс держал таск, упавший ещё до правок.

⚠️ Остаётся верным то, ради чего пункт и заводился: **`sync_cards` логирует
только когда `synced` или `pruned` больше нуля**. В простое лог молчит, и
живой таск неотличим от мёртвого. Если снова возникнет сомнение — смотреть не
в лог, а сверять `SELECT COUNT(*)` в обеих БД.

Файлы: `backend/app/vocab/mirror.py` (`periodic_sync`), `backend/app/main.py:113`.

### 0c. Устаревшие значения у слов, которые в базе ЕСТЬ

Замечено по дороге, отдельным замером не покрыто. `Nachhaltigkeit` лежит в
`vocab.db` с переводами «продолжительность, длительность, стойкость» — это
дословное значение из старого словаря. Современного и единственного нужного
для эссе («устойчивость, экологичность», sustainability) в сырых данных нет.

То есть пробел от источника до 1995 шире, чем «нет слова»: у части имеющихся
слов **нет современного значения**. Обогащение это частично лечит (модель
дописывает от себя), но `ru`/`ru_all` строятся на сырых переводах, и проверить
масштаб отдельно стоит.

Как мерить: взять слова, у которых сырые переводы есть, а Wiktionary даёт
заметно другое значение (пересечение глосс пустое), и посмотреть глазами на
выборке. Не запускать массовый requeue до замера — см. «Осознанно НЕ делаем».

### 0e. Карточка показывает пользователю сырые `**` — 11 260 карточек

Найдено 2026-07-20 при работе над дизайном карточки (ветка `cards-design`).
`js/app.js:421` рендерит немецкую строку примера через `md()` (промоутит
`**слово**` в `<b>`), а русскую — через `esc()`. Там, где модель разметила
ключевое слово и в переводе, читатель видит буквально:

> Я сейчас \*\*онлайн\*\* и могу с тобой пообщаться.

Замер по живой `enrichment.db`:

| поле | примеров/карточек с `**` | как рендерится сейчас |
|---|---|---|
| `examples[].de` | 276 075 из 276 270 (99.9 %) | `md()` — верно |
| `examples[].ru` | **33 508 из 276 270 (12.1 %)** | `esc()` — **сырые звёздочки** |
| `collocations` | **461 карточка (0.5 %)** | `esc()` — **сырые звёздочки** |

Итого **11 260 карточек** (12.2 % базы) показывают мусор хотя бы в одной
строке. Видно глазами на `prototypes/_baseline.html` для `online`.

Чинится одним вызовом на каждое поле (`esc` → `md`); в прототипах уже сделано,
в production **нет** — не трогал `app.js`, пока не выбран вариант карточки.
Учесть при переносе. Стили для `em` в переводе и коллокациях — в
`prototypes/cards.css`.

### 0f. Карточка — разный объект в зависимости от того, откуда открыта

Сетка `main` даёт левой колонке `minmax(400px,1.05fr)`, правой —
`minmax(340px,440px)` (`css/styles.css:93`). Карточка растягивается на ту
колонку, которую накрывает, поэтому на 1440 px измерено: **744 px** при
открытии из своего списка против **440 px** при открытии из поиска. Одно и то
же слово, один и тот же рендер, 1.7× разницы.

Плюс замер высоты production-карточки (`prototypes/_baseline.html`, тот же
`renderCardSheet`): `die Wirkung` — **972 px**, `entwickeln` — **1 188 px**,
при том что вьюпорт даёт 912 и карточка всегда скроллится внутри себя.

Лечится фиксацией меры (440 px — максимум, который держит узкая колонка). В
прототипах сделано и проверено: 436 px с обеих сторон во всех пяти вариантах.

### 0g. Блок «Grammatik» подписан там, где грамматики нет

У `online` и `durchaus` секция `Grammatik` состоит ровно из розовой панели
`Verwendung` с коллокациями — то есть заголовок обещает грамматику, а внутри
сочетаемость. Пустой `grammar: {}` — это 100 % наречий (1 271 из 1 271) и
несравнимые прилагательные.

Смежное: сегодняшняя таблица падежей одна на все существительные, а **44.2 %
существительных с парадигмой (22 226 из 50 311) имеют одну и ту же форму во
всех четырёх падежах Sg** — для них таблица печатает одно слово восемь раз.
Склоняется артикль, а его в таблице нет вовсе.

Разбор и решение — `prototypes/README.md` + `grammarModel()` в
`prototypes/cards.js` (форма блока зависит от части речи, блока нет там, где
нечего сказать). Ждёт выбора варианта владельцем.

### 0h. Страница: мазок растянут строкой, перевод обрезан, треть экрана пуста

Найдено 2026-07-20 на ветке `woerterbuch`. Три замера на 1440 px:

- **`css/styles.css:141` задаёт мазку `width:108%; height:165%` ОТ СТРОКИ.**
  Колонка поиска 744 px, поэтому компактное акварельное пятно растягивается
  примерно в 800 × 105 — это и есть «чиркаш». Размер должен задаваться
  **словом**, а не строкой и не константой: константа в 212 px свисает с
  `online` (85 px слова) на 130 px и еле накрывает `die Verantwortung` (230 px).
- **`css/styles.css:174` печатает перевод** 18 px курсивным serif в
  `--ink-soft` с `white-space:nowrap; text-overflow:ellipsis` поверх самой
  плотной части краски. Показывается ровно одно значение с многоточием, при
  том что больше одного значения у 77.2 % карточек.
- **Ряд «seltener» (`.syn-chip`, `styles.css:223`)** — 17 px serif без
  подложки и без зоны клика, подпись 10 px в `--muted-2` = **1.75 : 1**.
- **Сетка `main`** резервирует 440 px под карточку всегда, поэтому при
  744 px колонки поиска с ~360 px контента примерно треть страницы — пустой
  слот.

Решение и три концепции страницы — `prototypes/woerterbuch/`
(README.md, UX.md, ART-PROMPTS.md). Ждёт выбора варианта владельцем.

### 0i. Ранжирование: точное совпадение проигрывает алфавиту

Замечено при сборке прототипа поиска и **на бэкенде не проверено** — здесь
только гипотеза, которую стоит прогнать по живому `_by_relevance`
(`backend/app/vocab/search.py`).

В прототипе сортировка по алфавиту приводила к тому, что запрос `Wirkung`
отвечал `Auswirkung`, а само `Wirkung` уезжало вниз чипом «реже говорят» —
то есть точное слово, которое человек набрал, оказывалось подчинённым более
длинному композиту. Лечится приоритетом «точное → начинается с → содержит»
перед всеми прочими тайбрейками.

Проверить: `Wirkung`, `Herz`, `Recht`, `Art` — короткие леммы, у которых в
базе есть длинные композиты с тем же корнем.

### 1. `linken` всё ещё первый по запросу «обманывать»

Выше `täuschen` и `betrügen`. Это тот самый пример, которым мотивировался
`forms.py` — и он **не** починен.

Класс другой: словарь честно считает `linken` заголовочным словом (редкий
разговорный глагол), маркеров формы нет, поэтому `forms.py` его справедливо не
трогает. Но zipf 4.89 у него украден у прилагательного `link`/`links` — см.
[[wordfreq-zipf-is-case-insensitive]].

Напрашивающийся сигнал — **толщина словарной статьи**: у `linken` это
`["vt", "обманывать"]` из одного источника (20 символов), у `täuschen` — статья
из пяти источников на несколько килобайт. Замер уже есть: 14 446 карточек (19.1 %)
стоят на одном источнике с телом < 60 символов, из них 82 сидят выше zipf 4.0.

⚠️ Прежде чем внедрять — **померить на выборке**, не сломает ли это легитимные
редкие слова. Слепо доверять толщине статьи так же опасно, как слепо доверять
`form_of` из Wiktionary.

Файлы: `backend/app/vocab/search.py` (`_by_relevance`), `backend/app/vocab/forms.py`.

### 2. `fachsprachlich` — четверть базы со средним zipf 1.95

18 917 карточек. Для тренажёра эссе B1–C1 это шум, который разбавляет выдачу.
Сейчас никак не используется в ранжировании.

У Wiktionary есть `senses[].topics` — 57 доменов (medicine 1259, linguistics 872,
technology 773, botany 632, zoology 558, chemistry 435, anatomy 355…). Можно
поженить с нашим `register='fachsprachlich'` и опускать узкоспециальное при
равном score, как сделано с формами.

### 3. ✅ СДЕЛАНО — но НЕ так, как планировалось: CEFR из zipf считать нельзя

`unlisted` — 88 067 из 92 090 (**95.6 %**), b2 = 380, c1 = 195. Причина внешняя:
Goethe публикует списки только A1/A2/B1, официального B2/C1 не существует.
Значит `band` для 95.6 % базы — это не уровень, а заглушка «C1».

План был «считать полосу из zipf (стандартный прокси)». **Проверено на 4 023
карточках с настоящей разметкой Goethe — как замена CEFR не годится.**

Средние и медианы монотонны и намекают, что сигнал есть
(медиана a1 4.76 → b1 4.16 → c1 3.36 → c2 3.02). Но отдельные слова zipf не
разделяет — перекрытие огромно при любом пороге:

| порог | B1-слов ошибочно в C1 | C1-слов ошибочно в B1 |
|---|---|---|
| 3.0 | 121 / 3 312 (4 %) | **210 / 331 (63 %)** |
| 3.5 | 398 / 3 312 (12 %) | 120 / 331 (36 %) |
| 4.0 | **1 016 / 3 312 (31 %)** | 51 / 331 (15 %) |

И главное: **b1 = 4.16 против b2 = 4.15** — частотность не различает B1 и B2
вообще. Реальный обрыв только у c1/c2.

Вывод: посчитанный «B2» с 30 % ошибок ХУЖЕ честной заглушки. Заглушку видно как
заглушку, а посчитанный уровень выглядит как знание, и проверить его
пользователь не может.

**Сделано вместо этого:** `norm.freq_of()` считает полосу частотности и она
показывается **как частотность** («häufig / mittelhäufig / selten»), а не как
уровень. Настоящий уровень Goethe показывается тем 4 023, у кого он есть, и
другим стилем — `is-cefr` в рамке (ссылается на опубликованный список),
`is-freq` тихий и без рамки (наше чтение корпуса). Порог häufig = 4.0 не
произволен: там центр B1-лексики Goethe (медиана 4.16).

⚠️ `band` НЕ трогали — он остаётся ключом кисти (15 washes, новых не рисуем).
`freq` это отдельное поле и отдельное утверждение.

⚠️ `freq = None`, когда частоты нет, и UI тогда молчит. Пустая частота — не
доказательство редкости; дефолт «selten» был бы утверждением о слове, которое
мы не считали.

### 3b. `import_morphology.py` молча пишет в ПУСТЫШКУ (напоролись 2026-07-19)

Запуск без `--enrich-db` резолвит путь в заглушки `app/vocab/enrichment.db` /
`vocab.db`, а не в боевые `app/vocab/vocab_data/*`. Скрипт при этом **создаёт
в пустышке схему**, отрабатывает «успешно», прочитывает весь миллион строк
дампа и рапортует `stored 0 paradigms`.

Поймали только по строке `cards with a paradigm: 0 of 0` — знаменатель выдал
подмену. Будь в пустышке хоть одна карточка, результат выглядел бы как
«дамп не подошёл», и правильный вывод (перезалить парадигмы) не был бы сделан.

Это ровно та же ловушка, что уже описана для `intake.py` в `CRITICAL-LINKS.md`
§6a, но **защиту получил только `intake.py`**: там `known_lemmas` падает с
явным сообщением, а `import_morphology.py` остался без неё.

Чинить так же: падать, если в целевой БД ноль карточек, и печатать
разрешённые пути перед стартом. Пока не починено — **запускать только с явным
`--enrich-db`**.

⚠️ И запускать вообще: парадигмы **не** доезжают до новых слов сами. После
каждого вливания `import_morphology.py` надо гонять заново, иначе у свежих
карточек грамматики нет (было 0 из 15 725, стало 95 %).

### 4. Опечатка в сообщении коммита d4ab26f

Проскочил иероглиф: «27 929 真 NULL». Чинится только `git commit --amend` +
force-push поверх отправленного — **сам не делаю**, нужно добро владельца.

### 5. ✅ ПОЧИНЕНО — наречия без грамматики; requeue нужен десятку слов, не 1 800

Замерено 2026-07-19: пусто у **1 271 наречия из 1 271** (100 %) и у 546 из 571
`other`. Для сравнения — noun 3 из 55 822, verb 0 из 8 882, adj 387 из 9 811
(последнее корректно: `tot`, `schwanger` не степенуются).

Причина не в модели: в `_GRAMMAR_KEYS` **не было ключа `adv`**, а
`_norm_grammar` оставляет только перечисленные там ключи. Что бы модель ни
вернула для наречия, оно молча выбрасывалось.

Починено: добавлены `adv` (`komparativ`/`superlativ`) и `other`
(`kasus`/`deklination`/`zusammensetzung`), в промпт вписана таблица грамматики
по частям речи, `PROMPT_VERSION` → `enrich-2026-07-19a`.

**Осталось — сильно меньше, чем казалось.** Требование «requeue 1 800 карточек»
снято замером 19-го июля: фикс живой (сверены хост и контейнер), но **43
наречия, обогащённые уже новым промптом, тоже имеют пустую грамматику — и это
правильно**. Это `interessanterweise`, `phasenweise`, `samstagabends`,
`gottseidank`, `probehalber`: они не степенуются, степеней у них нет и взяться
им неоткуда.

То есть «100 % наречий без грамматики» — почти целиком **корректный** результат,
а не брак. Requeue-а стоят только градуируемые, и их горстка: `gern`, `gerne`,
`bald`, `oft`, `wohl` — все сидят на промптах 07-13…07-18 и действительно
пустые. Десяток слов, а не 1 800.

⚠️ Мерить долю пустой грамматики по `pos` целиком бессмысленно — знаменатель
почти весь состоит из слов, у которых грамматики и не должно быть. Смотреть
надо на конкретный список градуируемых наречий.

---

## 🔵 Обсуждено с владельцем 2026-07-19 (сленг, идиомы, клише, пары, уровни)

### F. ✅ СДЕЛАНО — клише для эссе подключены (1 748 шаблонов)

Таблица `phrases` в Postgres жива и заполнена, осиротела после удаления
`editor.html` (роутер `/api/phrases` был только его). Колонки ровно те, что
нужны: `text_de`, `translation_ru`, `essay_part`, `topic`, `level`.

| essay_part | штук |
|---|---|
| argument | 1 020 |
| schluss | 861 |
| gegenargument | 757 |
| einleitung | 755 |
| beispiel | 627 |
| ~~argument1~~ | 1 (опечатка в данных) |

Качество на выборке хорошее: «Zusammenfassend lässt sich sagen, dass …»,
«Ein zentrales Argument für … ist, dass …» — это настоящие шаблоны B2/C1.

⚠️ Но подмешаны и НЕ клише, а содержательные предложения по темам («In Schweden
wurde Mikroplastik … 2018 verboten»). Перед показом развести шаблон и пример —
это разные вещи для пишущего.

**Сделано 2026-07-19, ноль токенов.** Итог: **1 748 шаблонов** вместо 21
статического — einleitung 374, argument 541, gegenargument 339, schluss 264,
beispiel 230.

Что чинилось по дороге:

- **`GET /api/phrases` отдавал `[]`.** Fallback шёл на `topic IS NULL`, а таких
  строк ноль — все 4 021 привязаны к теме. Поэтому таблица и лежала мёртвой:
  достать что-то можно было, только зная точный слаг темы. Теперь fallback —
  на шаблоны.
- **Признак шаблона — многоточие, а не повторяемость.** 1 713 из 1 806
  уникальных текстов содержат `...`; остальные 93 — содержательные предложения
  под конкретную тему. Повторяемость слабее: самый частый шаблон лежит 120 раз
  (по разу на тему), но много одноразовых столь же переиспользуемы.
- **Дедупликация в пределах ЧАСТИ, не по всей таблице.** «Daraus ergibt sich die
  Frage, ob ...» значится и в `einleitung`, и в `argument`, и оба верны;
  глобальный DISTINCT отдавал текст одной части и молча укорачивал список
  другой (374 против 366).
- **Сортировка по длине.** Рядом с «Am Anfang steht ...» лежат тридцатисловные
  зачины, называющие свою исходную тему. Оба шаблоны, но пересаживается в чужое
  эссе только короткий.
- **Пагинатор рисовал кнопку на каждую страницу** — терпимо при двух, ломало
  колонку при 125. Теперь окно с многоточием; чинит заодно Wörterbuch и Analysen.
- Добавлен поиск по клише (немецкая и русская сторона), иначе 374 варианта по
  три на страницу — не листание, а лотерея.

Статический список из 21 клише оставлен как офлайн-запас: страница умеет
работать без бэкенда, и пишущий не должен терять формулировки из-за сбоя сети.

Файлы: `backend/app/services/phrases_repo.py` (`list_templates`),
`backend/app/api/routes/phrases.py` (`GET /api/phrases/templates`),
`js/schreiben.js` (`KLI_PARTS`, `loadKlischees`, `kliFiltered`, `renderPager`),
`css/schreiben.css`.

⚠️ Осталось: 93 текста без многоточия (содержательные предложения по темам) не
показываются вовсе. Их можно было бы отдавать как «примеры по теме», когда тема
эссе совпадает, — но это отдельная фича, не клише.

### G. Идиомы и сленг — нужен ДРУГОЙ склад, не таблица слов

- Сленг частично уже есть: `register='umgangssprachlich'` у 3 915 карточек.
  Это оценка модели, а не словарь сленга, но пустым местом тема не является.
- Идиомы: колонка `idioms` в `vocab.db` заполнена у **0 строк**. Пусто.
- Коллокации есть у 99 % карточек, но это сочетаемость, а не идиомы.

⚠️ **Ключевое ограничение:** идиомы и клише **многословны**, а многословные
леммы из таблицы слов осознанно исключены — в базе их было ровно 0 на 108 084
строки, и на это молча опираются карточка, личный список и подбор кисти (см.
`CRITICAL-LINKS.md`). Значит идиомы нельзя лить в `words`: им нужен свой склад,
и `phrases` для этого уже подходит по форме.

### H. ✅ ЗАКРЫТО пунктом 3 — дубль того же наблюдения

Настоящий уровень есть у 4 023 (b1 1 779, a1 863, a2 670, b2 380, c1 195,
c2 136), у остальных 88 067 (95.6 %) — заглушка. Сделано и, главное, **чем
именно заменено** — в пункте 3: частотность показывается как частотность,
считать из неё CEFR проверено и отвергнуто.

### I0. Украденная частотность — ГЛАВНАЯ болезнь, и она системная (2026-07-19)

Замер на новых карточках: из 688 с zipf ≥ 3.5 у **97 (14 %)** частотность
делится с другим словом. И почти все эти карточки **верны**:

| карточка | делит zipf с |
|---|---|
| `Wählen` = голосование | `wählen` = выбирать |
| `Gutes` = добро | `Gut` = имущество |
| `zuhause` = дома | `Zuhause` = дом |
| `klasse` = классный | `Klasse` = класс |
| `geschafft` = уставший | `schaffen` = создавать |
| `passiert` = протёртый | причастие от `passieren` |
| `Näher` = портной | `näher` = ближе |

`passierte Tomaten` и `der Näher` — настоящий немецкий, карточки не врут.
Врёт только частота: `wordfreq` не различает ни регистр, ни словоформу, поэтому
редкое значение садится на вес частого и всплывает над ним в поиске. Это ровно
та же болезнь, что у `linken` (пункт 1), просто теперь она измерена.

**Вывод для QA:** проверять карточки моделью — бить не по той цели. Фактического
брака среди новых слов единицы, а системная проблема одна и чинится
**детерминированно и бесплатно** в `_by_relevance`. Не удалять — слова настоящие.

**✅ Половина про регистр СДЕЛАНА (`467db8f`).** Проверка на выборке, которую
требовал этот пункт, дала неожиданный результат и переписала решение:
`Recht`/`recht`, `Morgen`/`morgen`, `Essen`/`essen`, `Leben`/`leben` **тоже
делят zipf побайтово** — как и все 275 пар с zipf ≥ 4.0 без единого исключения.
Значит «опускать при равной частоте» задело бы и законные пары; ни один признак
в данных не отличает `das Jetzt` от `der Morgen`, это пословная частотность
значения, которой у нас нет.

Сигнал нашёлся не в данных, а в запросе: **регистр, который набрал пользователь**.
«Morgen» просит существительное, «morgen» — наречие, и теперь оба отвечают верно.
Строчный или кириллический запрос откатывается на «сначала строчное», что верно
для служебных пар, составляющих основную массу класса. Обе карточки остаются в
выдаче соседними строками.

**Осталась вторая половина — словоформы**, где регистр не помогает: `passiert`
(протёртый) на весе причастия, `geschafft` (уставший), `Näher` (портной).
Здесь нужен сигнал «есть базовая форма с собственной карточкой», и он опаснее —
см. запрет на `form_of` из Wiktionary в «Осознанно НЕ делаем».

### I1. QA моделью: прогон Haiku 2026-07-19 задачу НЕ выполнил

135 пакетов по 60 карточек. Агент отчитался «все 135», но в том же отчёте
признал: подробно 0000–0020, дальше выборочно. В файле 11 вердиктов, то есть
покрытие ~15 %, а не 100 %. Плюс арифметика в отчёте не сходится («11 из
~13 500» при 8 066 карточках в пакетах).

Из 11 вердиктов подтверждено 7 (`Die`, `Mir`, `On`, `Jean`, `Tagesschau`,
`Paralympics`, `Guinness-Buch`). Отклонены `Pascal` (законная единица — паскаль),
`Einstein` («ein Einstein» = гений, лексикализовано), `listen` (редкий, но
настоящий глагол), `Jojo-Effekt` (допустимое написание).

Если повторять — давать агенту **узкий диапазон** (10–15 пакетов) и требовать
построчный отчёт по каждому пакету, чтобы выборочность была видна. Но сперва
стоит спросить, нужна ли эта проверка вообще: см. I0 — фактического брака мало.

### I. ✅ ЗАКРЫТО пунктом I0 — пары `ich`/`das Ich` работают

Среди новых слов 169 пар «заглавная/строчная», и большинство корректны:
`das Wählen` (голосование) против `wählen` (выбирать), `das Wohnen`, `das
Tanzen` — это настоящие субстантивированные инфинитивы.

Болезнь была не в карточках, а в частоте: **`wordfreq` не различает ни регистр,
ни словоформу**, поэтому `Wählen`-существительное наследует zipf 4.95 глагола
`wählen` и вставало в выдаче ВЫШЕ него. См.
[[wordfreq-zipf-is-case-insensitive]].

Предложение из этого пункта («опускать `noun`, у которого есть строчный
двойник») **проверено и отвергнуто** — оно задело бы `Morgen`, `Recht`, `Essen`,
`Leben`. Решение оказалось другим и лежит в I0: тайбрейк по регистру ЗАПРОСА
(`467db8f`).

### J. ⚠️ ГИПОТЕЗА НЕВЕРНА — requeue эти карточки НЕ чинит

План был: пересобрать бэкенд и переобогатить `Die`, `Mir`, `passiert` новым
промптом. **Проверено 19-го июля — не сработает.**

`Die` и `Mir` уже стоят на `prompt_version = enrich-2026-07-19a`, то есть
прошли новый промпт **и остались прежними**: `Die` = «чип», `Mir` = «мир
(ковёр)». Правило про редкое значение их не ловит, потому что карточки, строго
говоря, **не врут**: `das Die` (полупроводниковый кристалл) и `der Mir`
(персидский ковёр) — настоящие словарные значения. Промпт не может отличить
редкое-но-настоящее от неуместного, и требовать этого от него не стоит.

Ошибкой была не карточка, а её место в выдаче, и чинится это ранжированием, а
не токенами: после `467db8f` запрос «die» отвечает артиклем, «Die» — чипом.
Обе карточки на месте. См. I0.

Остаётся `passiert` (протёртый) — там регистр не помогает, это словоформа.
Вторая половина I0.

## 🟡 Подтверждено, ждёт исполнения

### A. ✅ СДЕЛАНО — влитые слова обогащены, риск не реализовался

Прогон завершён 19-го июля: **15 730 done, 3 380 skipped, 42 failed** из 19 152.

⚠️ Главный страх пункта — «модель начнёт массово скипать нормальную современную
лексику, потому что у новых строк тонкие данные» — **не подтвердился, и с
запасом**. Доля `skipped` у новых слов **17.6 %** против **28.6 %** у старой
базы: новые скипались *реже*, а не чаще. Смягчать skip-правило для источника
`wiktionary` не нужно.

Что ушло в skip, по убыванию частоты: `Von`, `An`, `AfD`, `US-Präsident`,
`Bild-Zeitung`, `3-Uhr-Zug`, `a-Laut`, `Dritte-Welt-Stadt` — заглавные дубли,
имена собственные, разовые композиты. Ровно то, что и должно отсеиваться.

Целевые слова из замера покрытия доехали все: `Internet` 5.23, `online` 5.38,
`aufgrund` 5.16, `Smartphone`, `Digitalisierung`, `Klimawandel`, `Privatsphäre`,
`Suchmaschine`, `Homeoffice`, `Migrationshintergrund`, и `Potenzial` — то самое
написание после 1996, которого не было (см. пункт 0). Поиск отдаёт их первыми.

**Повторный замер покрытия на том же корпусе** (пункт 0, те же `coverage*.py`):

| | было | стало |
|---|---|---|
| по токенам | 72.3 % | **76.4 %** |
| по типам | 65.5 % | **70.4 %** |

⚠️ Оставшиеся промахи мерены строже, чем есть на деле: в них сидят английские
вкрапления (`the`, `to`, `on`), сокращения (`usw`, `DSGVO`, `GG`), имена
(`Wikipedia`, `Beck`) и причастия (`veröffentlicht`, `untersucht`, `behandelt`).
Последние — **не** дыра: базовые глаголы в базе есть, и поиск их находит
триграммами, запрос `veröffentlicht` отвечает `veröffentlichen`. Замер этого не
видит, потому что сверяет леммы, а не ходит в поиск.

Заодно закрылся 0c: `Nachhaltigkeit` теперь «устойчивость», а не «длительность».

---

## 🟢 Бэклог — померено, не подтверждено

### B. Настоящее произношение вместо синтеза речи

**Самый дешёвый выигрыш из оставшихся.** Сейчас кнопка «Aussprache» на карточке
зовёт браузерный TTS. В дампе на наших карточках:

- **IPA: 59 038 из 59 335 (99.5 %)** — `[ˈvaʁtn̩]`
- **аудио: 58 300 (98.3 %)** — готовые ссылки на Wikimedia Commons, в т.ч. mp3:
  `https://upload.wikimedia.org/wikipedia/commons/transcoded/…/De-warten.ogg.mp3`

То есть живой носитель вместо робота, плюс транскрипция в карточку. Импорт — тем
же проходом, что и морфология, отдельная колонка. Ноль вызовов модели.

⚠️ Ссылки внешние: либо грузить с Commons (тогда офлайна нет и приватность
утекает на Wikimedia), либо качать ~58k ogg локально. Решение за владельцем.

### C. Семантическая сеть

На наших карточках: `hypernyms` 47.4 %, `synonyms` 41.7 %, `antonyms` 25.7 %,
`hyponyms` 23.7 %, `coordinate_terms` 25.4 %, `derived` 35.3 %.

`derived` — это Wortfamilie (`fahren` → `Fahrer`, `Abfahrt`, `befahren`), для
B1–C1 хороший способ набирать словарь. Антонимов у нас нет вообще.

### D. Слоговой перенос — 99.2 %

`hyphenations`. Мелочь, но для письма полезная и стоит ноль.

### E. Темы под эссе

Из аудита: `kuenstliche_intelligenz` = 1 карточка, `internet_soziale_medien` = 4,
`datenschutz_privatsphaere` = 3, `generationen` = 3 — при том что это ровно темы
Goethe B2/C1 Schreiben. Причина в [[vocab-source-is-pre-1995-dictionary]].

Закрывается пунктом A (новые слова) плюс перетегированием тем.

---

## ⛔️ Осознанно НЕ делаем

- **`form_of` из Wiktionary для опускания карточек.** Зовёт не-леммами 6 533 наши
  карточки, включая `das`, `es`, `ein`, `aber`, `was`, `werden`, `schon`, `mehr`.
  Даже строгий вариант забирает `alles`, `andere`, `weniger`, `nächste`.
- **Примеры из Wiktionary вместо примеров Mistral.** Их много (82 593 значения),
  но это корпусные цитаты уровня C2, а не учебные фразы. Реальный пример для
  `warten`: «Am Themenweg Almzeit warten packend inszenierte Bergerlebnisse auf
  der Skigebietsseite…». Наши три градуированных примера с переводом лучше.
- **Rektion из Wiktionary.** Категории вида «Verb mit Präposition auf + Akkusativ»
  существуют, но покрывают **122 записи** из 59 335. Управление остаётся за Mistral.
- **Массовый requeue по «подозрительной» частоте** — проверено 2026-07-17, см.
  `CRITICAL-LINKS.md` §8.
- **`confidence` как сигнал качества** — `high` у 76 306 из 76 332.
- **Считать уровень CEFR из zipf.** Проверено 2026-07-19 на 4 023 карточках с
  настоящей разметкой Goethe: при лучшем пороге 3.5 промах на 12 % B1-слов и
  36 % C1-слов, а B1 от B2 частотность не отличает вовсе (медианы 4.16 и 4.15).
  Показываем частотность КАК частотность — см. пункт 3. Соблазн вернуться к
  этому будет: средние по уровням монотонны и выглядят убедительно. Смотреть
  надо не на средние, а на перекрытие.

---

## ✅ Сделано

| Когда | Что | Коммит |
|---|---|---|
| 2026-07-19 | Обогащены влитые слова: +15 730 карточек, 92 090 всего; покрытие 72.3 → **76.4 %** | — |
| 2026-07-19 | Регистр запроса решает пары `Die`/`die`: артикль больше не проигрывает чипу | `467db8f` |
| 2026-07-19 | Парадигмы добиты на новые карточки: 48 403 → **63 340** (68.8 %), у новых 95 % | — |
| 2026-07-19 | Влито 20 074 новых слова из Wiktionary (`intake.py`), `vocab.db` 108 084 → 128 158 | — |
| 2026-07-19 | 25 служебных слов руками (`funcwords.py`), поиск отдаёт их первыми | — |
| 2026-07-19 | Замер покрытия на внешнем корпусе — вскрыл, что 82 % промахов от источника | — |
| 2026-07-19 | Парадигмы из Wiktionary: 48 403 карточки (63.4 %) | `66a336b` |
| 2026-07-19 | Пометка форм словаря, 1 676 карточек, поиск их опускает | `051ec0b` |
| 2026-07-19 | Золотой запас `enrichment.db` + правила хранения | `23e3bca` |
| 2026-07-19 | Чанкинг зеркала под лимит 32767 + `none_as_null` | `d4ab26f` |
| 2026-07-18 | Расклейка значений, починка пар/регистра/орфографии | §6a |
