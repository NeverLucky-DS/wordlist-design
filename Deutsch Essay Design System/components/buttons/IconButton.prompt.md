IconButton — square hairline control for a single icon (theme, folder, notes, pager arrows). Warms to accent on hover.

```jsx
<IconButton label="Theme" size="md"><SunIcon /></IconButton>
```

- `size`: `sm` (28px), `md` (38px), `lg` (46px).
- Always pass `label` for accessibility.
