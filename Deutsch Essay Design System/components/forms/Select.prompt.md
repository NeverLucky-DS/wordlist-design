Select — the pill dropdown trigger from the editor meta strip and niveau controls.

```jsx
<Select label="THEMA" value="Technologie" open={open} onClick={toggle} />
```

- `label`: optional tracked uppercase key.
- Renders the trigger button only — manage `open` state and render your own menu beneath it.
