ProgressBar — the writing-goal track with the warm accent gradient fill.

```jsx
<ProgressBar value={83} max={250} label="Fortschritt" showCount />
```
- `showCount` renders the serif "83 / 250 Wörter" cap above the rail.
