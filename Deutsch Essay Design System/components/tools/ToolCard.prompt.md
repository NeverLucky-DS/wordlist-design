ToolCard — the right-rail titled paper panel (Klischees, inline Wörterbuch, side tools).

```jsx
<ToolCard title="KLISCHEES" accent="ARGUMENT EINS" action={<Pager/>}>
  …list…
</ToolCard>
```
- `accent` is appended after a "·" in the accent-dark colour. `action` sits at the right of the head.
